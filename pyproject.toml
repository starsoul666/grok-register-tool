[project]
name = "grok-register-tool"
version = "0.1.0"
description = "Grok 账号注册工具"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "curl-cffi>=0.7.0",
    "DrissionPage>=4.0.0",
]
